{
  "buildCommand": "pnpm --filter @historibot/shared build && pnpm --filter @historibot/web build",
  "outputDirectory": "dist",
  "installCommand": "pnpm install",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
